name: budget_planner
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  # NAV + STATE (we'll wire go_router soon; tabs are local state first)
  go_router: ^14.2.0
  provider: ^6.1.2

  # UI niceties
  flutter_speed_dial: ^7.0.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

